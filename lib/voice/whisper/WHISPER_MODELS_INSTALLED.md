# Installed Faster-Whisper Caches

Ali currently has these local Faster-Whisper model caches under `lib/voice/whisper`:

- `tiny.en`
- `base.en`
- `small.en`
- `medium.en`
- `large-v3`

Phase 1C uses `small.en` by default for the live voice gate.
