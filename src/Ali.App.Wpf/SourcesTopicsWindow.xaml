<Window x:Class="Ali.App.Wpf.SourcesTopicsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sources &amp; Topics"
        Height="540"
        Width="880"
        MinHeight="460"
        MinWidth="760"
        Background="#0B0B0D"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#18181B" />
        <SolidColorBrush x:Key="InputBrush" Color="#0F0F11" />
        <SolidColorBrush x:Key="BorderBrushDark" Color="#323236" />
        <SolidColorBrush x:Key="TextBrush" Color="#F8FAFC" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#D0D5DD" />
        <SolidColorBrush x:Key="SubtleTextBrush" Color="#9DA4AE" />
        <Style TargetType="{x:Type Button}">
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="#2A2A2E" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="#46464D" />
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="Background" Value="{StaticResource InputBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="#46464D" />
            <Setter Property="CaretBrush" Value="{StaticResource TextBrush}" />
        </Style>
        <Style TargetType="{x:Type ComboBox}">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="Background" Value="{StaticResource InputBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="#46464D" />
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="{x:Type ComboBoxItem}">
                        <Setter Property="Background" Value="{StaticResource InputBrush}" />
                        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                        <Setter Property="Padding" Value="8,5" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#202C33" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#20313A" />
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Sources &amp; Topics"
                       Foreground="{StaticResource TextBrush}"
                       FontSize="24"
                       FontWeight="SemiBold" />
            <TextBlock x:Name="DescriptionText"
                       Foreground="{StaticResource MutedTextBrush}"
                       Margin="0,2,0,0" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{StaticResource BorderBrushDark}"
                    BorderThickness="1"
                    Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Button Grid.Row="0"
                            Content="New Source"
                            MinWidth="112"
                            HorizontalAlignment="Left"
                            Click="NewSourceButton_OnClick" />
                    <ListBox Grid.Row="1"
                             x:Name="SourcesListBox"
                             Margin="0,12,0,0"
                             Background="#111114"
                             BorderBrush="#323236"
                             Foreground="{StaticResource TextBrush}"
                             SelectionChanged="SourcesListBox_OnSelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="4">
                                    <TextBlock Text="{Binding Name}"
                                               Foreground="{StaticResource TextBrush}"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding Summary}"
                                               Foreground="{StaticResource SubtleTextBrush}"
                                               FontSize="12"
                                               TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Border Grid.Column="2"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{StaticResource BorderBrushDark}"
                    BorderThickness="1"
                    Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="170" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Source name"
                               Foreground="{StaticResource MutedTextBrush}"
                               FontWeight="SemiBold" />
                    <TextBlock Grid.Row="0"
                               Grid.Column="2"
                               Text="Trust"
                               Foreground="{StaticResource MutedTextBrush}"
                               FontWeight="SemiBold" />
                    <TextBox Grid.Row="1"
                             x:Name="NameTextBox"
                             Margin="0,6,0,12" />
                    <ComboBox Grid.Row="1"
                              Grid.Column="2"
                              x:Name="TrustComboBox"
                              Margin="0,6,0,12" />

                    <TextBlock Grid.Row="2"
                               Text="Address"
                               Foreground="{StaticResource MutedTextBrush}"
                               FontWeight="SemiBold" />
                    <TextBox Grid.Row="3"
                             Grid.ColumnSpan="3"
                             x:Name="UrlTextBox"
                             Margin="0,6,0,12"
                             ToolTip="Use an approved HTTP or HTTPS source address." />

                    <TextBlock Grid.Row="4"
                               Text="Topics"
                               Foreground="{StaticResource MutedTextBrush}"
                               FontWeight="SemiBold" />
                    <CheckBox Grid.Row="4"
                              Grid.Column="2"
                              x:Name="EnabledCheckBox"
                              Content="Use this source"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Right" />
                    <StackPanel Grid.Row="5" Grid.ColumnSpan="3">
                        <TextBox x:Name="TopicsTextBox"
                                 Margin="0,6,0,8"
                                 ToolTip="Separate topics with commas, such as weather, local forecast, Nashville." />
                        <TextBlock Text="Examples: weather, local forecast, software docs, sports scores"
                                   Foreground="{StaticResource SubtleTextBrush}"
                                   FontSize="12"
                                   Margin="0,0,0,12" />
                        <TextBlock Text="Notes"
                                   Foreground="{StaticResource MutedTextBrush}"
                                   FontWeight="SemiBold" />
                        <TextBox x:Name="NotesTextBox"
                                 Margin="0,6,0,0"
                                 MinHeight="92"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText"
                       Foreground="{StaticResource MutedTextBrush}"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Save"
                        MinWidth="88"
                        Click="SaveButton_OnClick" />
                <Button Content="Delete"
                        MinWidth="88"
                        BorderBrush="#7F1D1D"
                        Background="#2A1517"
                        Click="DeleteButton_OnClick" />
                <Button Content="Close"
                        MinWidth="88"
                        Margin="0"
                        Click="CloseButton_OnClick" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
